import React from 'react'
import { View, Text } from 'react-native'

export default class StudentAttendanceScreen extends React.Component {
    render() {
        return (
            <Text> attendance student</Text>
        )
    }
}